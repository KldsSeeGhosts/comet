diff --git a/libs/cua-driver/hyprland-plugin/src/foreground_route.hpp b/libs/cua-driver/hyprland-plugin/src/foreground_route.hpp
index 0b675d3..819a03a 100644
--- a/libs/cua-driver/hyprland-plugin/src/foreground_route.hpp
+++ b/libs/cua-driver/hyprland-plugin/src/foreground_route.hpp
@@ -62,10 +62,10 @@ struct ForegroundSeatBindings {
     unsigned primary_candidates = 0;
 
     void observe(std::string_view resource_class, bool plugin_owned) {
-        if (resource_class == "wl_seat" && !plugin_owned && primary_candidates < 2)
+        if (resource_class == "wl_seat" && !plugin_owned)
             ++primary_candidates;
     }
-    bool unique() const { return primary_candidates == 1; }
+    bool available() const { return primary_candidates > 0; }
 };
 
 inline ForegroundFailureReason foreground_key_modifier_failure(const std::array<std::uint32_t, 4>& modifiers) {
diff --git a/libs/cua-driver/hyprland-plugin/src/input_experiment.cpp b/libs/cua-driver/hyprland-plugin/src/input_experiment.cpp
index fc7e740..df84955 100644
--- a/libs/cua-driver/hyprland-plugin/src/input_experiment.cpp
+++ b/libs/cua-driver/hyprland-plugin/src/input_experiment.cpp
@@ -224,7 +224,7 @@ struct InputExperiment::Impl {
     bool foreground_keyboard_used = false;
     bool foreground_needs_pointer = false;
     WP<CWLSurfaceResource> foreground_surface;
-    WP<CWLSeatResource> foreground_seat;
+    std::vector<WP<CWLSeatResource>> foreground_seats;
     std::vector<WP<CWLPointerResource>> foreground_pointers;
     std::vector<WP<CWLKeyboardResource>> foreground_keyboards;
     std::array<std::uint32_t, 4> foreground_modifiers{};
@@ -727,36 +727,49 @@ struct InputExperiment::Impl {
     bool point(Client& c, double x, double y) const {
         return std::isfinite(x) && std::isfinite(y) && x >= 0 && y >= 0 && x < c.geometry[2] && y < c.geometry[3];
     }
-    bool unique_primary_seat(wl_client* client) const {
+    std::vector<WP<CWLSeatResource>> primary_seats(wl_client* client) const {
         struct Scan {
             const Impl* self;
-            ForegroundSeatBindings bindings;
+            std::vector<WP<CWLSeatResource>> seats;
         } scan{this, {}};
-        // Hyprland's public lookup returns only the first primary binding.
-        // Count classes without casting arbitrary protocol user data. Agent
-        // seats are excluded only by exact resource identity, never by name.
+        // A client may bind the compositor's one primary wl_seat global more
+        // than once. Firefox does this through separate registries. Keep every
+        // pre-existing primary binding and exclude agent seats by exact resource
+        // identity, never by a user-visible name.
         wl_client_for_each_resource(client, [](wl_resource* resource, void* data) {
             auto& scan = *static_cast<Scan*>(data);
-            const std::string_view resource_class = wl_resource_get_class(resource);
-            if (resource_class != "wl_seat") return WL_ITERATOR_CONTINUE;
-            bool owned = false;
+            if (std::string_view{wl_resource_get_class(resource)} != "wl_seat")
+                return WL_ITERATOR_CONTINUE;
             for (const auto* peer : scan.self->peers) {
                 if (!peer) continue;
                 for (const auto& seat : peer->seats)
-                    if (seat->wl->resource() == resource) { owned = true; break; }
-                if (owned) break;
+                    if (seat->wl->resource() == resource) return WL_ITERATOR_CONTINUE;
             }
-            scan.bindings.observe(resource_class, owned);
+            if (const auto seat = CWLSeatResource::fromResource(resource); seat && seat->good())
+                scan.seats.push_back(seat);
             return WL_ITERATOR_CONTINUE;
         }, &scan);
-        if (!scan.bindings.unique()) return false;
+        return scan.seats;
+    }
+    bool primary_seats_stable(wl_client* client) const {
+        const auto seats = primary_seats(client);
+        if (seats.empty()) return false;
         if (!foreground_started) return true;
-        // A release/rebind or new child resource during a drag must not leave
-        // our captured delivery set different from the primary seat's set.
-        const auto seat = g_pSeatManager->seatResourceForClient(client);
-        if (!seat || seat != foreground_seat.lock()) return false;
-        return foreground_resources_match(seat->m_pointers, foreground_pointers) &&
-            foreground_resources_match(seat->m_keyboards, foreground_keyboards);
+        if (!foreground_resources_match(seats, foreground_seats)) return false;
+        std::vector<WP<CWLPointerResource>> pointers;
+        std::vector<WP<CWLKeyboardResource>> keyboards;
+        for (const auto& weak : seats) {
+            const auto seat = weak.lock();
+            if (!seat || !seat->good()) return false;
+            for (const auto& pointer : seat->m_pointers)
+                if (pointer && pointer->good()) pointers.push_back(pointer);
+            for (const auto& keyboard : seat->m_keyboards)
+                if (keyboard && keyboard->good()) keyboards.push_back(keyboard);
+        }
+        // A release, rebind, or new child resource during a drag must not leave
+        // the captured delivery set different from the client's current set.
+        return foreground_resources_match(pointers, foreground_pointers) &&
+            foreground_resources_match(keyboards, foreground_keyboards);
     }
     ForegroundGuard foreground_guard(const Client& c) const {
         const auto root = c.surface.lock();
@@ -769,7 +782,7 @@ struct InputExperiment::Impl {
         const bool exact_root = geometry && *geometry == c.geometry;
         return {
             .exact_root = exact_root,
-            .primary_binding = !exact_root || unique_primary_seat(root->client()),
+            .primary_binding = !exact_root || primary_seats_stable(root->client()),
             .peer_conflict = agent_conflict(c),
             .physical_keys = pressed,
             .physical_buttons = g_pInputManager->hasHeldButtons(),
@@ -810,7 +823,7 @@ struct InputExperiment::Impl {
                     k->sendMods(foreground_modifiers[0], foreground_modifiers[1], foreground_modifiers[2], foreground_modifiers[3]);
                 }
         held_button = 0; held_keys.clear();
-        foreground_pointers.clear(); foreground_keyboards.clear(); foreground_surface.reset(); foreground_seat.reset();
+        foreground_seats.clear(); foreground_pointers.clear(); foreground_keyboards.clear(); foreground_surface.reset();
         foreground_started = false;
         foreground_keyboard_used = false;
     }
@@ -824,11 +837,16 @@ struct InputExperiment::Impl {
         if (needs_pointer && !g_pSeatManager->m_mouse) throw ForegroundFailure{ForegroundFailureReason::physical_pointer};
         const Vector2D local{x + c.geometry[0] - c.geometry[4], y + c.geometry[1] - c.geometry[5]};
         if (needs_pointer && (!point(c, x, y) || root->at(local, true).first != root)) throw ForegroundFailure{ForegroundFailureReason::pointer_target};
-        const auto seat = g_pSeatManager->seatResourceForClient(root->client());
-        if (!seat || !seat->good()) throw ForegroundFailure{ForegroundFailureReason::seat_resource};
+        const auto seats = primary_seats(root->client());
+        if (seats.empty()) throw ForegroundFailure{ForegroundFailureReason::seat_resource};
+        foreground_seats = seats;
         foreground_pointers.clear(); foreground_keyboards.clear();
-        for (const auto& p : seat->m_pointers) if (p && p->good()) foreground_pointers.push_back(p);
-        for (const auto& k : seat->m_keyboards) if (k && k->good()) foreground_keyboards.push_back(k);
+        for (const auto& weak : seats) {
+            const auto seat = weak.lock();
+            if (!seat || !seat->good()) throw ForegroundFailure{ForegroundFailureReason::seat_resource};
+            for (const auto& p : seat->m_pointers) if (p && p->good()) foreground_pointers.push_back(p);
+            for (const auto& k : seat->m_keyboards) if (k && k->good()) foreground_keyboards.push_back(k);
+        }
         if (needs_pointer && foreground_pointers.empty()) throw ForegroundFailure{ForegroundFailureReason::pointer_resources};
         if (foreground_keyboards.empty()) throw ForegroundFailure{ForegroundFailureReason::keyboard_resources};
         foreground_modifiers = {physical->m_modifiersState.depressed, physical->m_modifiersState.latched,
@@ -845,7 +863,6 @@ struct InputExperiment::Impl {
         }
         xkb_state_update_mask(keyboard_state, foreground_modifiers[0], foreground_modifiers[1], foreground_modifiers[2], 0, 0, foreground_modifiers[3]);
         foreground_surface = root;
-        foreground_seat = seat;
         foreground_needs_pointer = needs_pointer;
         c.foreground_attempted = true;
         foreground_started = true;
diff --git a/libs/cua-driver/hyprland-plugin/tests/foreground_route_test.cpp b/libs/cua-driver/hyprland-plugin/tests/foreground_route_test.cpp
index bdac3d3..3f6cd12 100644
--- a/libs/cua-driver/hyprland-plugin/tests/foreground_route_test.cpp
+++ b/libs/cua-driver/hyprland-plugin/tests/foreground_route_test.cpp
@@ -80,17 +80,16 @@ int main() {
     bindings.observe("wl_keyboard", false);
     bindings.observe("wl_seat", true); // Cua-Agent
     bindings.observe("wl_seat", true); // Cua-Agent-2
-    check(!bindings.unique());
+    check(!bindings.available());
     bindings.observe("wl_seat", false); // Primary binding
-    check(bindings.unique());
-    // A second binding, including one created during a drag, must refuse.
+    check(bindings.available());
+    // Firefox and derivatives may bind the same primary global more than once.
     bindings.observe("wl_seat", false);
-    check(!bindings.unique());
+    check(bindings.available());
     bindings.observe("wl_seat", true);
-    check(!bindings.unique());
-    // Saturation cannot make an arbitrarily large resource count unique.
+    check(bindings.available());
     for (unsigned i = 0; i < 1000; ++i) bindings.observe("wl_seat", false);
-    check(!bindings.unique());
+    check(bindings.available());
 
     std::array<std::uint32_t, 4> modifiers{};
     constexpr std::array modifier_reasons{ForegroundFailureReason::keyboard_depressed,
